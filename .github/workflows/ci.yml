name: CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

jobs:
  # ── Frontend unit tests (Jest + TypeScript) ─────────────────
  jest:
    name: Jest Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'

      - run: npm ci

      - name: Install SDK test dependencies
        run: npm ci --prefix sdk/js

      - name: Run Jest unit tests (xfuel-sdk)
        run: npm test --prefix sdk/js -- --ci --coverage=false

  # ── Solidity compile + core contract tests ──────────────────
  contracts-core:
    name: Core Contract Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - name: Compile contracts
        run: npx hardhat compile

      - name: Generate TypeChain types
        run: npm run typechain

      - name: Core listener (node:test)
        run: npm run test:contracts:core:listener

      - name: Core contracts + security (Hardhat)
        run: npm run test:contracts:core:solidity

  # ── Cypress E2E: disabled until wallet mocking is set up ────
  # These require Theta Wallet / Keplr browser extensions and are
  # best run locally or in a dedicated browser-automation environment.
  # Uncomment and configure when a wallet mock layer is added.
  # e2e:
  #   name: Cypress E2E
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with: { node-version: '20', cache: 'npm' }
  #     - run: npm ci
  #     - run: npm run dev &
  #     - run: npm run test:e2e:headless
  #       env: { CYPRESS_baseUrl: 'http://localhost:3000' }


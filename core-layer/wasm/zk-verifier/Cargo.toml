[package]
name = "xfuel-zk-verifier"
version = "0.1.0"
edition = "2021"
description = "XFuel Core Layer — CosmWasm ZK Verifier for SP1 proofs on Cosmos chains"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []
# Enable library mode for dependency usage (no entry_point exports)
library = []

[dependencies]
cosmwasm-std = { version = "1.5", features = ["stargate"] }
cosmwasm-storage = "1.5"
cw-storage-plus = "1.2"
cw2 = "1.1"
schemars = "0.8"
serde = { version = "1.0", default-features = false, features = ["derive"] }
thiserror = "1.0"
hex = "0.4"

[dev-dependencies]
cosmwasm-schema = "1.5"

[profile.release]
opt-level = 3
debug = false
rpath = false
lto = true
debug-assertions = false
codegen-units = 1
panic = "abort"
incremental = false
overflow-checks = true

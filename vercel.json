{
  "buildCommand": "cd xfuel-app && npm install && tsc -b && npx vite build",
  "outputDirectory": "xfuel-app/dist",
  "installCommand": "npm install --legacy-peer-deps",
  "env": {
    "VITE_MAINTENANCE": "false"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

chore: fix Slither config and run first internal security scan

- Fix slither.config.json: convert filter_paths and detectors_to_exclude
  from JSON arrays to comma-separated strings (required by slither-analyzer 0.11.x)
- Add scripts/parse-slither.py to parse and triage slither-report.json by severity
- Add slither-report.json: first full scan of 117 contracts, 349 findings
  (37 High, 45 Medium, 237 Low, 14 Informational)
- Notable High findings in contracts/core/: reentrancy-eth in
  CoreRevenueSplitter.distribute() and _routeStake(), incorrect-equality in
  veXFGovernance.votingPower()/vote(), uninitialized-state ZKVerifierSP1.rollupBatchSize
- .cursorrules: document pip3 workaround (use python -m pip on Windows) and
  correct Slither invocation (slither . from project root, not contracts/core/)
